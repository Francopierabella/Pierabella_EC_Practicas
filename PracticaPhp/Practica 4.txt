Practica 4 PHP

PHP: variables, tipos, operadores, expresiones, estructuras de control
1) 
Las variables que se ven en el código son: a de tipo bool, b de tipo string, c de tipo string, d de tipo int, f de tipo int, g de tipo int, i de tipo int. 
Los operadores que vemos son: el operador de asignación =, el de incremento ++, el de asignación y suma +=, el de multiplicación *.
Las funciones que aparecen son las siguientes: doble, que lleva como parámetro un entero, gettype, que lleva como parámetro cualquier tipo y devuelve el tipo de variable, is_int, que lleva como parámetro cualquier tipo y devuelve True o False.
Las estructuras de control que vemos son: el condicional if. 
Y la salida por la pantalla es: booleanstringstringinteger1xyzxyz184444

2)
(a). Los códigos son equivalentes.
b. Los códigos son equivalentes.
c. Los códigos son equivalentes.

3)
a. El código lo que hace es generar una tabla con 2 columnas y 5 filas cada una usando un for, 
con esto podemos modificar el valor de las variables row y col para generar una tabla con cualquier valor16:19 8/9/2025. 
También tiene un ancho de 90% y el borde de 1px.

b. Este código inicia con una condición si la variable _POST['submit'] no esta seteada, en ese caso se ejecuta el formulario.
 El formulario tiene como action una llamada a la ruta donde esta ubicado el mismo archivo php y tiene el método post, que significa que los datos no estarán en la url.
  Luego sigue con una casilla de texto y un botón. Después del condicional, en caso de que este seteado el valor del boton, 
  guarda el valor de la casilla de texto en la variable age y compara si es mayor a 21. En caso verdadero muestra un texto que dice "mayor de edad" , 
  en caso contrario muestra "menor de edad".

4) 
En la primer linea, antes del include, la salida del código será "El " y nos dará una advertencia sobre las variables flor y color que no están definidas. 
Después de incluir a el archivos datos.php nos mostrara "El clavel banco".

5)
El código es un contador de visitas que funciona con un archivo .dat. 
El archivo contador.php lo que hace es abrir el archivo, leer el contenido para guardarlo en el programa y luege abrirlo nuevamente para agregar la nueva visita. 
Al final lo que hace es mostrar la cantidad de visitas que tuvo. En el archivo visitas.php lo que hace es llamar al contador.php.
Un detalle que veo es que en contador.php le falta al inicio "<?php", probando el código sin eso no funciona correctamente el programa.

PHP: arrays, funciones 
1)
Los códigos son equivalentes. Muestran un array con distintos valores.

2)
a. La salida es bar1, el true lo toma como un 1.
b. La salida es 5942. Hay un array dentro de otro.
c. No hay salida en este código, aparte se les borra los valores a el arreglo.

3)
a. "Has entrado en esta pagina a las 22 horas, con 59 minutos y 30 segundos, del 7/9/2025"
b. "5+6=11"

4)

<form action="" method="get">
    Nombre: <input type="text" name="usuario">
    <input type="submit" value="Enviar">
</form>
<?php
if(isset($_GET["usuario"]) && $_GET["usuario"]!=""){
    comprobar_nombre_usuario($_GET["usuario"]);
}
else{
    echo "";
}
?>